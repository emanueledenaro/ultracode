name: Validate

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Validate repository structure
        run: python scripts/validate_repository.py

      - name: Validate deterministic project configurator
        run: python plugins/ultracode/skills/ultracode/scripts/run_project_configurator_corpus.py

      - name: Validate UltraCode contract with Python
        run: python plugins/ultracode/skills/ultracode/scripts/check_contract.py

      - name: Validate UltraCode contract with PowerShell
        shell: powershell
        run: >-
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File
          plugins/ultracode/skills/ultracode/scripts/check_contract.ps1
