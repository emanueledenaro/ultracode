interface:
  display_name: "UltraCode Edit"
  short_description: "Safely edit UltraCode project control"
  default_prompt: "Use $ultracode-edit to change the UltraCode project configuration and regenerate only managed adapters."
policy:
  allow_implicit_invocation: false
