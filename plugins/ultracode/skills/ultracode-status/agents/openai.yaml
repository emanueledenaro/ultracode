interface:
  display_name: "UltraCode Status"
  short_description: "Explain detailed task state and evidence"
  default_prompt: "Use $ultracode-status to explain the complete task state, model and effort routing, job history, files, findings, checks, blockers, and next action."
policy:
  allow_implicit_invocation: true
