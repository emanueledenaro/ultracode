interface:
  display_name: "UltraCode Status"
  short_description: "Show clear live AI work status"
  default_prompt: "Use $ultracode-status to show where the AI work is, which agent jobs are active, and what happens next."
policy:
  allow_implicit_invocation: true
