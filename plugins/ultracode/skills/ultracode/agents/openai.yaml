interface:
  display_name: "UltraCode"
  short_description: "Transparent adaptive engineering workflow"
  default_prompt: "Use $ultracode to execute this engineering task with visible progress, objective-driven model and effort routing, safe project initialization when required, and verified results."
policy:
  allow_implicit_invocation: true
