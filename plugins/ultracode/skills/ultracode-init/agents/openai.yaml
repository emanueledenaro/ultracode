interface:
  display_name: "UltraCode Init"
  short_description: "Configure projects with guided control"
  default_prompt: "Use $ultracode-init to configure this repository for transparent UltraCode work in Codex and Claude Code."
policy:
  allow_implicit_invocation: false
