---
description: Inspect this repository and propose UltraCode project control (writes only after confirmation)
argument-hint: "[scope or constraint]"
---

Use the `ultracode-init` skill from the ultracode plugin.

Additional context: $ARGUMENTS

Explain in plain language what guidance you propose, why each file is useful, and exactly what will
change, then wait for confirmation before any write. This session is Claude Code: make sure the
proposal includes the managed `CLAUDE.md` projection that imports the canonical `AGENTS.md`.
