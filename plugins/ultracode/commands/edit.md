---
description: Change UltraCode project configuration as an explained before-and-after delta
argument-hint: "<what to change>"
---

Use the `ultracode-edit` skill from the ultracode plugin.

Requested change: $ARGUMENTS

Show the delta before and after, detect conflicts and manual edits, and update only the affected
managed content after confirmation. Never overwrite drift you have not explained.
